# ============================================
# Frontend Environment Variables
# Copy this file to ".env" and fill real values
# React only reads vars prefixed with REACT_APP_
# ============================================

# Backend API base URL (Ingress path or LoadBalancer DNS in production)
REACT_APP_API_URL=http://localhost:5000
